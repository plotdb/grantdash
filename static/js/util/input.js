// Generated by LiveScript 1.3.0
var editableInput;
editableInput = function(node){
  var v, idx, ref$, n;
  if (node.childNodes.length > 1) {
    ldCaret.setContent("\u200b");
    v = node.innerText;
    idx = (ref$ = v.indexOf("\u200b")) > 0 ? ref$ : 0;
    v = v.replace("\u200b", "");
    node.innerText = v;
    n = node.childNodes[0];
    if (n) {
      ldCaret.set({
        ns: n,
        ne: n,
        os: idx,
        oe: idx
      });
    }
    node.focus();
  } else {
    v = node.innerText;
  }
  return v;
};