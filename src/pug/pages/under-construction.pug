extends /base.pug
block vars
  - ctrl.navtop.placeholder = false;
  - ctrl.navtop.shown = false;
  - ctrl.navtop.className = "navbar-light"
  // include /design/data/sample.pug
block head
  +css("https://cdn.jsdelivr.net/npm/tail.datetime@0.4.13/css/tail.datetime-harx-light.min.css")
block body
  //- landing page with domain, org found, but no landing page
  .w-100.h-100
    .w-640.mx-auto.h-100: .vertical-center: .w-100.text-center.pb-4
      .text-sm.text-muted 歡迎來到
      h2#domain.mb-2.font-weight-bold.ld.ld-float-btt-in
      p.text-muted.mb-4 這裡目前暫時還沒有提供任何資訊，請過段時間再回來。
      br
      .btn.btn-sm.btn-outline-secondary 主控台
      +nbr(3)
  script: :lsc
    document.querySelector(\#domain).innerText = window.location.host
