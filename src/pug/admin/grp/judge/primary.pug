//- module

style(type="text/css"): :stylus
  div[ld-scope=judge-primary]
    .progress-bar
      transition: .35s width ease-in-out
  div[ld-scope=judge-primary] div[ld=ldbar]
    svg path { stroke-width: 9 }
    svg path.mainline { stroke: #09f }
    .ldBar-label {
      font-size: 1.5em;
      text-align: center
    }
    .ldBar-label:after {
      font-size: .5em;
    }
    .ldBar-label:before {
      font-size: .6em;
      content: "整體進度";
      display: block
    }

+scope("judge-primary")
  .rwd.mx-auto.typeset.heading-contrast.mt-0
    h3 篩選作業
    p.text-muted 篩選由指定的評審進行，主要用於快速篩選提案使用。完成後，可視情形接續評分作業。
    a.btn.btn-outline-primary.mr-2(ld="primary-user-link",target="_blank",rel="noopener noreferrer",
    href="#") 進行審查
    a.btn.btn-outline-primary(ld="primary-all-link",target="_blank", rel="noopener noreferrer",
    href="#") 瀏覽總表
    hr
    .row
      .col-md
        .form-group.d-flex.align-items-center
          .flex-grow-1
            div 過濾評選結果
            .text-muted.text-sm 評選表僅列出依資格審查通過的專案
          div: .switch.switch-lg(ld="switch",data-name="filter-criteria")
      .col-md
        .form-group.d-flex.align-items-center
          .flex-grow-1
            div 評審匿名
            .text-muted.text-sm 若啟用，總表將會顯示評審名稱
          div: .switch.switch-lg(ld="switch",data-name="anonymous")
    .row
      .col-md
        .form-group.d-flex.align-items-center
          .flex-grow-1
            div 選項類型
            .text-muted.text-sm 快選的選項項目與文字
          div
            select.form-control(ld="choice",data-name="option-type")
              option(value="") 推薦、面議、汰除
              option(value="2way") 推薦、汰除
      .col-md

    hr
    .row.py-4
      .col-md-5: .px-4
        .aspect-ratio.ratio-1by1
          .ldBar.w-100.h-100.label-center(ld="ldbar",data-value="0",data-preset="circle")
      .col-md-7
        h4 個別評選進度
        hr
        .d-none.my-2(ld-each="primary-judge"): .d-flex.align-items-center
          .mr-2(href="#",style="flex:0 0 10em",ld="name",target="_blank", rel="noopener noreferrer")
          .flex-grow-1(ld="progress")
            .progress
              .progress-bar.bg-success(ld="progress-bar",data-name="0",style="width:50%")
              .progress-bar.bg-warning(ld="progress-bar",data-name="1",style="width:20%")
              .progress-bar.bg-danger(ld="progress-bar",data-name="2",style="width:10%")
